# Publishes `static_home/` to GitHub Pages (static HTML/CSS).
# Before the first deploy succeeds: Repo → Settings → Pages → Build and deployment →
# Source → GitHub Actions (not “Deploy from a branch”). We intentionally omit
# actions/configure-pages here: it calls the Pages API and fails with 404 until Pages
# is enabled, which blocks the upload/deploy steps that would otherwise work.
name: Deploy static home to GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - "static_home/**"
      - ".github/workflows/deploy-static-home.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upload static_home as site root
        uses: actions/upload-pages-artifact@v3
        with:
          path: static_home

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
